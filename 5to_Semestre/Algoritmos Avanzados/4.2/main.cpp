#include <iostream>

#include <vector>

using namespace std;

//struct for point
struct Point {
    int x;
    int y;
};

//function which implement the algorithm to determine the smallest convex polygon in a set of points by applying Graham's Scan algorithm 
//returns array of points (vertices) in counterclockwise order that define the convex polygon
//input: array of points (vertices)
//output: array of points (vertices) in counterclockwise order that define the convex polygon
//precondition: array of points (vertices) is not empty
//postcondition: array of points (vertices) in counterclockwise order that define the convex polygon
//complexity: O(n)
vector<int>

int main(){
    

    return 0;
}
